#!/usr/bin/env bash
set -euo pipefail
_gpg="$(dirname "$0")/_gpg"
_get_details="$(dirname "$0")/_get_details"

options="$1" keychain="$2" name="$3" account="$4" url="$5"

"${_gpg}" --decrypt "${keychain}/${name}" | "${_get_details}" "${name}"
