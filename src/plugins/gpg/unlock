#!/usr/bin/env bash
set -euo pipefail
_gpg="$(dirname "$0")/_gpg"
_gpg_encrypt="$(dirname "$0")/_gpg_encrypt"

options="$1" keychain="$2"

[[ -p /dev/stdin ]] && IFS= read -r PW_KEYCHAIN_PASSWORD
echo | "${_gpg_encrypt}" "${options}" | "${_gpg}" --decrypt >/dev/null
