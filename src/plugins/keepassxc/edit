#!/usr/bin/env bash
set -euo pipefail
_keepassxc_cli_with_args="$(dirname "$0")/_keepassxc_cli_with_args"

keychain="$1" password="$2" name="$3" account="$4" url="$5"

"${_keepassxc_cli_with_args}" edit --password-prompt "${keychain}" "${name}" << EOF
${PW_KEYCHAIN_PASSWORD}
${password}
EOF
