#!/usr/bin/env bash
# shellcheck disable=SC2046
shellcheck $(
	find "src" "plugins" -type f &&
	find "test/fixtures" -type f -name "*.bash" &&
	find "test" -maxdepth 1 -type f -name "*.bash" -or -name "*.bats"
)
