pw::prepare_keychain() { :; }
pw::plugin_init()      { exit 1; }
pw::plugin_add()       { exit 1; }
pw::plugin_edit()      { exit 1; }
pw::plugin_get()       { exit 1; }
pw::plugin_show()      { exit 1; }
pw::plugin_rm()        { exit 1; }
pw::plugin_ls()        { exit 1; }
pw::plugin_open()      { exit 1; }
pw::plugin_lock()      { exit 1; }
pw::plugin_unlock()    { exit 1; }
